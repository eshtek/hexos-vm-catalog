{
  "name": "hexos-vm-catalog-tools",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Validation tooling for the HexOS VM blueprint catalog. Lives under _lib/ so the catalog sync (which ingests every non-underscore *.json in the repo root) never sees it.",
  "scripts": {
    "validate": "bun validate.ts",
    "sync-schema": "bash sync-schema.sh",
    "check-sources": "bun check-sources.ts",
    "releases": "bun releases.ts",
    "tests": "bun tests.ts",
    "generate-tests": "bun tests.ts --generate",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
